[package]
name = "gate_campaign_example"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = { version = "28.0.0" }

[dev-dependencies]
soroban-sdk = { version = "28.0.0", features = ["testutils", "hazmat-address"] }
gate_claim = { path = "../gate_claim" }
test_mt = { path = "../test_mt" }
gate_battery = { path = "../gate_battery" }
gate_ticket = { path = "../gate_ticket" }
