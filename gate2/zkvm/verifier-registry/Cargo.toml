[package]
name = "verifier-registry"
version = "0.1.0"
edition = "2021"
rust-version = "1.97.1"
license = "LicenseRef-PolyForm-Shield-1.0.0"
description = "Generic RoleId-based staking + slashing registry for the prover's multi-domain L1. Master Verifier, Relayer, Attester, and any future role share one primitive."

[lints]
workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.11"
hex = "0.4"

