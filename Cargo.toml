[workspace]
members = [
    "crates/gate_vm",
  "gate2/soroban/gate_claim",
  "gate2/soroban/gate_campaign_example",
  "contracts/finality_registry",
  "contracts/settlement_gateway",
  "crates/source_simulator",
  "crates/relayer",
  "crates/domain_adapter",
  "crates/execution_vm",
  "gate2/soroban/gate_claim",
  "gate2/soroban/gate_campaign_example",
]
resolver = "2"

# Contracts require overflow checks in release: silent integer wrapping in a
# finality/settlement contract is a correctness bug, not a performance tradeoff.
[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true
